import React, { Fragment } from 'react';

/**
 * Rich-Text Editors, sometimes called ‘what you see is what you get’ (WYSIWYG) editors, 
 * allow you to create documents that include text, images, charts, grids and videos. 
 * They are powerful programs and apps, that can be used as standalone applications or 
 * as web-based components
 * 
 * @param {*} value
 * @param {*} handleChange
 * @returns component fragment
 */
const FormTextEditorField = ({ value, handleChange }) => {

    return (
        <Fragment>
            Editor
        </Fragment>
    );

}

export default FormTextEditorField;