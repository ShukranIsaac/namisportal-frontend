import React, { Fragment } from 'react';

export const CreateDirectoryInstitution = ({
    institution,
    handleChange,
    handleClick,
}) => {

    return (
        <Fragment>
            No institution
        </Fragment>
    );

}