import React, { Fragment } from 'react';

export const EditDirectoryInstitution = ({
    institution,
    handleChange,
    handleClick,
}) => {

    return (
        <Fragment>
            No institution data
        </Fragment>
    );

}